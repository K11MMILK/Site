@*@model First_site_V2.Storage.Entities.Community

<div class="main">
    <div class="container">
        <div class="row">
            <div style="float: left; width: 25%;height:300px;">
                <form action =@Model.PNG target="_blank">
                    <button >
                        <img src=@Model.PNG height="275" width="275"/>
                    </button>
                </form>
            </div>

            <div style="float: right; width: 72%;height:300px;">
                <div class="col-2-3">
                    <h2>@Model.Name </h2>
                    
                    <form asp-controller="Commun" asp-action="JoinToCommunity" method="get">
                    <input type="hidden" name="id" value="@Model.Id">
                    <input type="hidden" name="a" value="1">
                    <button type = "submit">Вступить в сообщество</button>
                    </form>
                    <form asp-controller="Commun" asp-action="DeleteCommunity" method="get">
                    <input type="hidden" name="id" value="@Model.Id">
                    <button type = "submit">Удалить сообщество</button>
                    </form>
                </div>
            </div>
             <div class="row">
            <div style="float: left; width: 25%;height:300px;">
                    
                <form asp-controller="Commun" asp-action="Members" method="post">
                    <input type="hidden" name="id" value="@Model.Id">
                    <button type = "submit">Показать участников</button>
                </form>
                <form asp-controller="Commun" asp-action="NewPost" method="get">
                    <input type="hidden" name="communityId" value="@Model.Id">
                    <button type = "submit">Добавить пост</button>
                </form>
                    @if (Model.HasPosts == false)
                    {
                        <p>У данного сообщества нет постов</p>
                    }
                    else{
                    <form asp-controller="Commun" asp-action="AllPosts" method="post">
                    <input type="hidden" name="id" value="@Model.Id">
                    <button>Показать все посты</button>
                    </form>
                    }
        </div>
      </div>
                    
                </div>       
    </div>
</div>*@
@model First_site_V2.Storage.Entities.Community

<div class="main">
    <div class="container">
        <div class="row">
            <div style="float: left; width: 25%;height:300px;">
                <form action =@Model.PNG target="_blank">
                    <button >
                        <img src=@Model.PNG height="275" width="275"/>
                    </button>
                </form>
            </div>

            <div style="float: right; width: 72%;height:300px;">
                <div class="col-2-3">
                    <h2>@Model.Name </h2>

                    <form asp-controller="Commun" asp-action="JoinToCommunity" method="get">
                        <input type="hidden" name="id" value="@Model.Id">
                    <button>Вступить в сообщество</button>
                    </form>
                </div>
            </div>
             <div class="row">
            <div style="float: left; width: 25%;height:300px;">

                <form asp-controller="Commun" asp-action="Members" method="post">
                    <input type="hidden" name="id" value="@Model.Id">
                    <button>Показать участников</button>
                    </form>
                    <form asp-controller="Commun" asp-action="NewPost" method="get">
                    <input type="hidden" name="communityId" value="@Model.Id">
                    <button type = "submit">Добавить пост</button>
                </form>
                    @if (Model.HasPosts == false)
                    {
                        <p>У данного сообщества нет постов</p>
                    }
                    else{
                    <form asp-controller="Commun" asp-action="AllPosts" method="post">
                    <input type="hidden" name="id" value="@Model.Id">
                    <button>Показать все посты</button>
                    </form>
                    }
        </div>
      </div>
                    }
                </div>
    </div>
</div>