@model First_site_V2.Storage.Entities.Community

<div class="main">
    <div class="container">
        <div class="row">
            <div style="float: left; width: 25%;height:300px;">
                <form action =@Model.PNG target="_blank">
                    <button >
                        <img src=@Model.PNG height="275" width="275"/>
                    </button>
                </form>
            </div>

            <div style="float: right; width: 72%;height:300px;">
                <div class="col-2-3">
                    <h2>@Model.Name </h2>
                    
                    <form asp-controller="Commun" asp-action="ToCommunity" method="get">
                        <input type="hidden" name="id" value="@Model.Id">
                        <input type="hidden" name="a" value="1">
                    <button>Вступить в сообщество</button>
                    </form>
                </div>
            </div>
             <div class="row">
            <div style="float: left; width: 25%;height:300px;">
                    @foreach(var member in Model.Members)
                    {
                        <div class="col d-flex align-items-start">
        <div class="icon-square bg-dark text-white d-inline-flex align-items-center justify-content-center fs-4 flex-shrink-0 me-3">
          <img src=@member.PNG height="70" width="70" />
        </div>
        <div>
          <h2>@member.Name @member.Surname</h2>
          @*<form method="get" action="/PersonProfile/PersonProfile">
            <input type="hidden" name="login" value="@member.Login">
            <button type="submit">Перейти на профиль</button><br /><br />
        </form>*@
        </div>
      </div>
                    }
                </div>
        </div>
        </div>
       
    </div>
</div>